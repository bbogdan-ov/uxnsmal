Program {
    reset_func: None,
    funcs: {
        UniqueName(0): Function {
            is_vector: true,
            body: [],
        },
        UniqueName(1): Function {
            is_vector: false,
            body: [],
        },
        UniqueName(2): Function {
            is_vector: false,
            body: [
                Label(UniqueName(29)),
            ],
        },
        UniqueName(3): Function {
            is_vector: false,
            body: [
                Short(4),
                Byte(0),
                JumpIf(UniqueName(31)),
                Jump(UniqueName(32)),
                Label(UniqueName(31)),
                Intrinsic(Pop, IntrMode(SHORT)),
                Jump(UniqueName(30)),
                Label(UniqueName(32)),
                Intrinsic(Pop, IntrMode(SHORT)),
                Jump(UniqueName(30)),
                Label(UniqueName(30)),
                Short(10),
                Intrinsic(Pop, IntrMode(SHORT)),
            ],
        },
        UniqueName(4): Function {
            is_vector: false,
            body: [
                Short(4),
                Byte(0),
                JumpIf(UniqueName(34)),
                Jump(UniqueName(35)),
                Label(UniqueName(34)),
                Intrinsic(Pop, IntrMode(SHORT)),
                Jump(UniqueName(33)),
                Label(UniqueName(35)),
                Intrinsic(Pop, IntrMode(SHORT)),
                Jump(UniqueName(33)),
                Label(UniqueName(33)),
                Short(10),
                Intrinsic(Pop, IntrMode(SHORT)),
            ],
        },
        UniqueName(5): Function {
            is_vector: false,
            body: [
                Intrinsic(Pop, IntrMode(0x0)),
                Jump(UniqueName(36)),
                Label(UniqueName(36)),
                Byte(10),
                Short(123),
                Intrinsic(Pop, IntrMode(SHORT)),
            ],
        },
        UniqueName(6): Function {
            is_vector: false,
            body: [
                Short(30),
                Byte(0),
                JumpIf(UniqueName(38)),
                Jump(UniqueName(39)),
                Label(UniqueName(38)),
                Intrinsic(Pop, IntrMode(SHORT)),
                Jump(UniqueName(37)),
                Label(UniqueName(39)),
                Intrinsic(Pop, IntrMode(SHORT)),
                Jump(UniqueName(37)),
                Label(UniqueName(41)),
                Label(UniqueName(40)),
                Label(UniqueName(37)),
                Byte(123),
                Intrinsic(Pop, IntrMode(0x0)),
            ],
        },
        UniqueName(7): Function {
            is_vector: false,
            body: [
                Jump(UniqueName(42)),
                Label(UniqueName(44)),
                Label(UniqueName(43)),
                Label(UniqueName(42)),
                Byte(123),
                Intrinsic(Pop, IntrMode(0x0)),
            ],
        },
        UniqueName(8): Function {
            is_vector: false,
            body: [
                Byte(10),
                Byte(0),
                JumpIf(UniqueName(47)),
                Jump(UniqueName(48)),
                Label(UniqueName(47)),
                Jump(UniqueName(46)),
                Label(UniqueName(48)),
                Intrinsic(Pop, IntrMode(0x0)),
                Jump(UniqueName(45)),
                Label(UniqueName(46)),
                Intrinsic(Pop, IntrMode(0x0)),
                Label(UniqueName(45)),
                Byte(123),
                Intrinsic(Pop, IntrMode(0x0)),
            ],
        },
        UniqueName(9): Function {
            is_vector: false,
            body: [
                Short(0),
                Intrinsic(Dup, IntrMode(SHORT)),
                Short(2),
                Intrinsic(Mul, IntrMode(SHORT)),
                Intrinsic(Dup, IntrMode(SHORT)),
                Short(2),
                Intrinsic(Eq, IntrMode(SHORT)),
                JumpIf(UniqueName(51)),
                Intrinsic(Dup, IntrMode(SHORT)),
                Short(8),
                Intrinsic(Eq, IntrMode(SHORT)),
                JumpIf(UniqueName(53)),
                Jump(UniqueName(54)),
                Label(UniqueName(53)),
                Jump(UniqueName(50)),
                Label(UniqueName(54)),
                Jump(UniqueName(52)),
                Label(UniqueName(51)),
                Jump(UniqueName(50)),
                Label(UniqueName(52)),
                Intrinsic(Dup, IntrMode(SHORT)),
                Short(10),
                Intrinsic(Gth, IntrMode(SHORT)),
                JumpIf(UniqueName(55)),
                Jump(UniqueName(56)),
                Label(UniqueName(55)),
                Intrinsic(Pop, IntrMode(SHORT)),
                Jump(UniqueName(49)),
                Label(UniqueName(56)),
                Label(UniqueName(50)),
                Intrinsic(Pop, IntrMode(SHORT)),
                Intrinsic(Inc, IntrMode(SHORT)),
                Label(UniqueName(49)),
                Intrinsic(Pop, IntrMode(SHORT)),
            ],
        },
        UniqueName(10): Function {
            is_vector: false,
            body: [
                Byte(0),
                JumpIf(UniqueName(57)),
                Jump(UniqueName(58)),
                Label(UniqueName(57)),
                Label(UniqueName(58)),
            ],
        },
        UniqueName(11): Function {
            is_vector: false,
            body: [
                Byte(0),
                JumpIf(UniqueName(59)),
                Jump(UniqueName(60)),
                Label(UniqueName(59)),
                Intrinsic(Pop, IntrMode(0x0)),
                Byte(2),
                Label(UniqueName(60)),
            ],
        },
        UniqueName(12): Function {
            is_vector: false,
            body: [
                JumpIf(UniqueName(61)),
                Jump(UniqueName(62)),
                Label(UniqueName(61)),
                Label(UniqueName(62)),
                Byte(8),
            ],
        },
        UniqueName(13): Function {
            is_vector: false,
            body: [
                Byte(0),
                JumpIf(UniqueName(63)),
                Jump(UniqueName(64)),
                Label(UniqueName(63)),
                Label(UniqueName(64)),
            ],
        },
        UniqueName(14): Function {
            is_vector: false,
            body: [
                Byte(0),
                JumpIf(UniqueName(65)),
                Short(65535),
                Jump(UniqueName(66)),
                Label(UniqueName(65)),
                Short(30),
                Label(UniqueName(66)),
            ],
        },
        UniqueName(15): Function {
            is_vector: false,
            body: [
                JumpIf(UniqueName(67)),
                Byte(2),
                Short(65535),
                Jump(UniqueName(68)),
                Label(UniqueName(67)),
                Byte(1),
                Short(30),
                Label(UniqueName(68)),
            ],
        },
        UniqueName(16): Function {
            is_vector: false,
            body: [
                Intrinsic(Dup, IntrMode(0x0)),
                Byte(10),
                Intrinsic(Gth, IntrMode(0x0)),
                JumpIf(UniqueName(69)),
                Jump(UniqueName(70)),
                Label(UniqueName(69)),
                Byte(0),
                JumpIf(UniqueName(71)),
                Byte(2),
                Intrinsic(Mul, IntrMode(0x0)),
                ShortAddrOf(UniqueName(0)),
                Jump(UniqueName(72)),
                Label(UniqueName(71)),
                Byte(1),
                Intrinsic(Add, IntrMode(0x0)),
                ShortAddrOf(UniqueName(0)),
                Label(UniqueName(72)),
                Intrinsic(Pop, IntrMode(SHORT)),
                Label(UniqueName(70)),
            ],
        },
        UniqueName(17): Function {
            is_vector: false,
            body: [
                Intrinsic(Dup, IntrMode(0x0)),
                Byte(10),
                Intrinsic(Gth, IntrMode(0x0)),
                JumpIf(UniqueName(73)),
                Byte(0),
                JumpIf(UniqueName(75)),
                Byte(2),
                Intrinsic(Mul, IntrMode(0x0)),
                ShortAddrOf(UniqueName(0)),
                Jump(UniqueName(76)),
                Label(UniqueName(75)),
                Byte(1),
                Intrinsic(Add, IntrMode(0x0)),
                ShortAddrOf(UniqueName(0)),
                Label(UniqueName(76)),
                Jump(UniqueName(74)),
                Label(UniqueName(73)),
                Byte(3),
                Intrinsic(Div, IntrMode(0x0)),
                ShortAddrOf(UniqueName(0)),
                Label(UniqueName(74)),
            ],
        },
        UniqueName(18): Function {
            is_vector: false,
            body: [
                Label(UniqueName(77)),
                Byte(0),
                JumpIf(UniqueName(78)),
                Jump(UniqueName(79)),
                Label(UniqueName(78)),
                Jump(UniqueName(77)),
                Label(UniqueName(79)),
            ],
        },
        UniqueName(19): Function {
            is_vector: false,
            body: [
                Byte(0),
                Label(UniqueName(80)),
                Intrinsic(Dup, IntrMode(0x0)),
                Byte(10),
                Intrinsic(Lth, IntrMode(0x0)),
                JumpIf(UniqueName(81)),
                Jump(UniqueName(82)),
                Label(UniqueName(81)),
                Intrinsic(Dup, IntrMode(0x0)),
                Byte(2),
                Intrinsic(Mul, IntrMode(0x0)),
                Intrinsic(Pop, IntrMode(0x0)),
                Intrinsic(Inc, IntrMode(0x0)),
                Jump(UniqueName(80)),
                Label(UniqueName(82)),
                Intrinsic(Pop, IntrMode(0x0)),
            ],
        },
        UniqueName(20): Function {
            is_vector: false,
            body: [
                Intrinsic(Sth, IntrMode(SHORT)),
                Short(0),
                Label(UniqueName(83)),
                Intrinsic(Dup, IntrMode(SHORT)),
                Intrinsic(Sth, IntrMode(KEEP | RETURN | SHORT)),
                Intrinsic(Lth, IntrMode(SHORT)),
                JumpIf(UniqueName(84)),
                Jump(UniqueName(85)),
                Label(UniqueName(84)),
                Intrinsic(Inc, IntrMode(SHORT)),
                Jump(UniqueName(83)),
                Label(UniqueName(85)),
                Intrinsic(Pop, IntrMode(RETURN | SHORT)),
            ],
        },
        UniqueName(21): Function {
            is_vector: false,
            body: [
                Byte(0),
                Label(UniqueName(87)),
                Intrinsic(Dup, IntrMode(0x0)),
                Byte(10),
                Intrinsic(Lth, IntrMode(0x0)),
                JumpIf(UniqueName(88)),
                Jump(UniqueName(89)),
                Label(UniqueName(88)),
                Intrinsic(Dup, IntrMode(0x0)),
                Byte(5),
                Intrinsic(Eq, IntrMode(0x0)),
                JumpIf(UniqueName(90)),
                Jump(UniqueName(91)),
                Label(UniqueName(90)),
                Jump(UniqueName(86)),
                Label(UniqueName(91)),
                Intrinsic(Inc, IntrMode(0x0)),
                Jump(UniqueName(87)),
                Label(UniqueName(89)),
                Label(UniqueName(86)),
                Intrinsic(Pop, IntrMode(0x0)),
            ],
        },
        UniqueName(22): Function {
            is_vector: false,
            body: [
                Byte(0),
                Label(UniqueName(93)),
                Intrinsic(Dup, IntrMode(0x0)),
                Byte(10),
                Intrinsic(Lth, IntrMode(0x0)),
                JumpIf(UniqueName(94)),
                Jump(UniqueName(95)),
                Label(UniqueName(94)),
                Intrinsic(Dup, IntrMode(0x0)),
                Byte(5),
                Intrinsic(Eq, IntrMode(0x0)),
                JumpIf(UniqueName(96)),
                Jump(UniqueName(97)),
                Label(UniqueName(96)),
                Intrinsic(Pop, IntrMode(0x0)),
                Jump(UniqueName(92)),
                Label(UniqueName(97)),
                Intrinsic(Inc, IntrMode(0x0)),
                Jump(UniqueName(93)),
                Label(UniqueName(95)),
                Intrinsic(Pop, IntrMode(0x0)),
                Label(UniqueName(92)),
            ],
        },
        UniqueName(23): Function {
            is_vector: true,
            body: [
                Return,
            ],
        },
        UniqueName(24): Function {
            is_vector: true,
            body: [
                Return,
            ],
        },
        UniqueName(25): Function {
            is_vector: true,
            body: [
                Byte(2),
                Byte(0),
                JumpIf(UniqueName(98)),
                Jump(UniqueName(99)),
                Label(UniqueName(98)),
                Intrinsic(Pop, IntrMode(0x0)),
                Return,
                Label(UniqueName(99)),
                Byte(4),
                Intrinsic(Add, IntrMode(0x0)),
                Intrinsic(Pop, IntrMode(0x0)),
            ],
        },
        UniqueName(26): Function {
            is_vector: false,
            body: [
                Return,
            ],
        },
        UniqueName(27): Function {
            is_vector: false,
            body: [
                Return,
            ],
        },
        UniqueName(28): Function {
            is_vector: false,
            body: [
                Byte(0),
                JumpIf(UniqueName(100)),
                Jump(UniqueName(101)),
                Label(UniqueName(100)),
                Byte(1),
                Return,
                Label(UniqueName(101)),
                Byte(0),
                JumpIf(UniqueName(102)),
                Jump(UniqueName(103)),
                Label(UniqueName(102)),
                Byte(69),
                Return,
                Label(UniqueName(103)),
                Byte(0),
            ],
        },
    },
    vars: {},
    consts: {},
    datas: {},
}