Program {
    reset_func: None,
    funcs: {
        UniqueName(0): Function {
            is_vector: false,
            body: [],
        },
        UniqueName(1): Function {
            is_vector: false,
            body: [
                Label(UniqueName(14)),
            ],
        },
        UniqueName(2): Function {
            is_vector: false,
            body: [
                Intrinsic(Pop, IntrMode(0x0)),
                Byte(10),
                Label(UniqueName(15)),
            ],
        },
        UniqueName(3): Function {
            is_vector: false,
            body: [
                Short(2),
                Label(UniqueName(16)),
            ],
        },
        UniqueName(4): Function {
            is_vector: false,
            body: [
                Jump(UniqueName(17)),
                Label(UniqueName(17)),
            ],
        },
        UniqueName(5): Function {
            is_vector: false,
            body: [
                Short(2),
                Jump(UniqueName(18)),
                Label(UniqueName(18)),
            ],
        },
        UniqueName(6): Function {
            is_vector: false,
            body: [
                Jump(UniqueName(19)),
                Label(UniqueName(19)),
            ],
        },
        UniqueName(7): Function {
            is_vector: false,
            body: [
                Byte(0),
                JumpIf(UniqueName(21)),
                Jump(UniqueName(22)),
                Label(UniqueName(21)),
                Jump(UniqueName(20)),
                Label(UniqueName(22)),
                Label(UniqueName(20)),
            ],
        },
        UniqueName(8): Function {
            is_vector: false,
            body: [
                Short(30),
                Byte(0),
                JumpIf(UniqueName(24)),
                Jump(UniqueName(25)),
                Label(UniqueName(24)),
                Intrinsic(Pop, IntrMode(SHORT)),
                Jump(UniqueName(23)),
                Label(UniqueName(25)),
                Intrinsic(Pop, IntrMode(SHORT)),
                Label(UniqueName(23)),
            ],
        },
        UniqueName(9): Function {
            is_vector: false,
            body: [
                Short(30),
                Byte(0),
                JumpIf(UniqueName(27)),
                Jump(UniqueName(28)),
                Label(UniqueName(27)),
                Jump(UniqueName(26)),
                Label(UniqueName(28)),
                Label(UniqueName(26)),
                Intrinsic(Pop, IntrMode(SHORT)),
                Short(5),
            ],
        },
        UniqueName(10): Function {
            is_vector: false,
            body: [
                Short(0),
                Intrinsic(Dup, IntrMode(SHORT)),
                Short(2),
                Intrinsic(Mul, IntrMode(SHORT)),
                Intrinsic(Dup, IntrMode(SHORT)),
                Short(2),
                Intrinsic(Eq, IntrMode(SHORT)),
                JumpIf(UniqueName(31)),
                Intrinsic(Dup, IntrMode(SHORT)),
                Short(8),
                Intrinsic(Eq, IntrMode(SHORT)),
                JumpIf(UniqueName(33)),
                Jump(UniqueName(34)),
                Label(UniqueName(33)),
                Jump(UniqueName(30)),
                Label(UniqueName(34)),
                Jump(UniqueName(32)),
                Label(UniqueName(31)),
                Jump(UniqueName(30)),
                Label(UniqueName(32)),
                Intrinsic(Dup, IntrMode(SHORT)),
                Short(10),
                Intrinsic(Gth, IntrMode(SHORT)),
                JumpIf(UniqueName(35)),
                Jump(UniqueName(36)),
                Label(UniqueName(35)),
                Intrinsic(Pop, IntrMode(SHORT)),
                Jump(UniqueName(29)),
                Label(UniqueName(36)),
                Label(UniqueName(30)),
                Intrinsic(Pop, IntrMode(SHORT)),
                Intrinsic(Inc, IntrMode(SHORT)),
                Label(UniqueName(29)),
                Intrinsic(Pop, IntrMode(SHORT)),
            ],
        },
        UniqueName(11): Function {
            is_vector: true,
            body: [
                Byte(4),
                Intrinsic(Pop, IntrMode(0x0)),
                Return,
            ],
        },
        UniqueName(12): Function {
            is_vector: false,
            body: [
                Byte(1),
                Return,
            ],
        },
        UniqueName(13): Function {
            is_vector: false,
            body: [
                Intrinsic(Dup, IntrMode(SHORT)),
                Short(2),
                Intrinsic(Eq, IntrMode(SHORT)),
                JumpIf(UniqueName(37)),
                Jump(UniqueName(38)),
                Label(UniqueName(37)),
                Intrinsic(Pop, IntrMode(SHORT)),
                Byte(1),
                Return,
                Label(UniqueName(38)),
                Short(8),
                Intrinsic(Eq, IntrMode(SHORT)),
                JumpIf(UniqueName(39)),
                Jump(UniqueName(40)),
                Label(UniqueName(39)),
                Byte(2),
                Return,
                Label(UniqueName(40)),
                Byte(20),
            ],
        },
    },
    vars: {},
    consts: {},
    datas: {},
}