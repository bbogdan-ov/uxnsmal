// Labeled blocks
fun t1( byte -- byte ) {
	// No block
}
fun t2( byte -- byte ) {
	@block {}
}
fun t3( byte -- byte ) {
	@block { pop 10 }
}
fun t4( byte -- byte short ) {
	2* @block {}
}

fun t5( byte -- byte ) {
	@exit { jump @exit }
}
fun t6( byte -- byte short ) {
	@exit { 2* jump @exit }
}
fun t7( byte -- byte ) {
	@exit {
		jump @exit
		3* // dead code
	}
}

fun t8( byte -- byte ) {
	@exit { 0 if { jump @exit } }
}
fun t9( byte -- byte ) {
	@exit {
		30*
		0 if {
			pop jump @exit
			1 // dead code
		}
		pop
	}
}
fun t10( byte -- byte short ) {
	30*
	@exit {
		0 if { jump @exit }
	}
	pop 5*
}

fun t11( byte -- byte ) {
	0* /* idx */
	loop @exit {
		dup 2* mul /* n */
		@skip {
			/* n */ dup 2* eq if {
				jump @skip
			} else {
				/* n */ dup 8* eq if { jump @skip }
			}

			dup 10* gth if { /* n */ pop jump @exit }
		}
		/* idx n */ pop
		/* idx */ inc
	}
	/* idx */ pop
}

fun t12( -> ) {
	4 pop
	return
	10 // dead code
}

fun t13( -- byte ) {
	1 return
	30* 50*
}

fun t14( short -- byte ) {
	dup 2* eq if { pop 1 return }
		8* eq if {     2 return }
	20
}
